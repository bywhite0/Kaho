# 更新日志
## 0.7.0 - 2026-06-06

- 数据存储重构为文件缓存方案，移除对 SQLite 数据库的运行时依赖。
- `/dbrebuild` 改为清空缓存并重建文件索引，重建后仍会刷新运行时数据。
- 版本同步改为按版本号与文件哈希增量刷新，删除的 YAML 会自动清理对应缓存。
- 优化初始化性能：版本同步与重建阶段不再全量解析 YAML，显著降低内存和 CPU 峰值。
- 新增 `/bgp_icon` 命令，支持可选 `frame` 参数（默认 `bgp`），可将同条消息图片居中裁切为 1:1 后进行圆形裁切与头像框叠加，并支持透明 GIF 输入输出。

### 技术改动
- 引入 `FileDB`、`MasterDataCacheManager`、`MusicChartCache` 三层组件，统一落盘到 `data/llll`。
- 删除 `src/core/services/db/*` 与 SQLAlchemy 相关实现。
- YAML 解析优先使用 `CSafeLoader` 流式读取，仅在解析失败时回退 sanitizer。
- 新增 `scripts/profile_runtime_memory.py`，可分阶段采集 DataManager 的内存峰值与耗时。
- 更新测试用例，移除 `KAHO_DB_PATH` 与 `_engine` 依赖，增加缓存删除清理与文件变更回归。

## 0.6.0 - 2026-03-28

- 新增 `/live_detail [--spoiler]`，可生成指定场次直播详情图。
- `/live` 补充 Fes×LIVE 展示，文案与信息显示更完整。
- 直播详情补充更多字段展示，支持更多实用信息。

### 技术改动
- 登录前自动拉取最新 `client version` 与 `res version`，降低版本不匹配导致的请求失败。
- DataManager 新增 `Costume` 与 `LiveLocation` 数据接入，并优化关闭数据库时机。

## 0.5.0 - 2026-03-26

- 新增 `/live` 命令，可直接查看当前 With×MEETS/Fes×LIVE 图片。
- 卡牌页头与素材展示重构，技能与追加卡牌展示更清晰。
- `music` 页面补充期数与气氛信息，模板样式同步优化。

### 技术改动
- 修复 `dbrebuild` 与运行时缓存刷新问题，提升数据加载幂等性。
- 统一记录渲染降级异常日志，补充模板渲染与真实数据测试。

## 0.4.0 - Unreleased

- 新增 `/update with_live`，可刷新 `archive/get_home` 并缓存最新一条 archive 详情。
- 补充游戏 API 凭据读取与会话自动刷新，减少手动维护成本。
- 增加缓存备份与详情回退策略，接口波动时也能保留可用结果。

### 技术改动
- 增加 with_live 数据刷新服务与缓存写入流程。

## 0.3.2 - 2026-03-11

- 技能展示改为结构化内容，阅读体验更稳定。
- 支持多追加卡牌与间接引用，补全技能关联链路。
- 卡牌技能区域版式优化，减少大段文本堆叠。

### 技术改动
- DataManager 拆分并接入搜索、限流、舞台与熟练度 mixin，减少重复实现。
- 搜索链路增加索引与限流能力，改善高频查询表现。

## 0.3.1 - 2026-03-11

- 优化 `music`、`comic` 命令的查询体验与提示反馈。
- 优化卡牌查询索引与命令逻辑，减少无效匹配等待。

### 技术改动
- 提升 T2I 服务稳定性与资源释放行为。
- 拆分音乐分析模板宏并修复视口问题，模板维护成本更低。

## 0.3.0 - 2026-03-11

- 数据库管理升级为 SQLAlchemy ORM，数据同步与重建更稳定。
- 迁移项目数据存储方案，移除 `localstore` 依赖。

### 技术改动
- 同步依赖配置与锁文件，统一数据库访问层实现。

## 0.2.2 - 2026-03-01

- 卡面、图标与部分元素视觉升级，信息层级更直观。
- 修复 `music` 命令将 6 位数以下纯数字误判为 ID 的问题。
- 修复部分场景下图片资源加载失败的问题。

### 技术改动
- 优化 SQLite 连接与数据写入流程，引入 YAML 哈希校验后仅写入变更文件。
- 完善 README 与运行说明，统一 Python 版本表述。

## 0.2.1 - 2026-02-24

- 歌曲 Quest Live 小节新增技能图标展示。

## 0.2.0 - 2026-02-24

- 图片输出改为 Jinja2 模板渲染，页面结构更统一。
- 补充漫画标签信息与命令可见性限制。

### 技术改动
- 切换到 OneBot V11 适配器。

## 0.1.0 - 2026-02-13
- 初始版本发布。
